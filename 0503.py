int() 정수형 추출 함수

튜플개념제대로알기
return x, y

x, y = function(a)  // return반환값이 2개면 정의도 2개
func_two(x , y)



/ 는 나누기

% 는 나머지

// 는 몫

** 는 거듭제곱




from itertools import product  nPr
    list(product(*items))
from itertools import permutations nPr
    list(permutations(items, 2))
from itertools import combinations nCr
    list(combinations(items, 2))